meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  installDependencies:
    - npm install --legacy-peer-deps
  
run:
  persistWorkDir: true
  command: npm start
```

---

### ШАГ 2: ОБНОВИТЬ .NPMRC

Проверь что файл `.npmrc` существует:
```
legacy-peer-deps=true
